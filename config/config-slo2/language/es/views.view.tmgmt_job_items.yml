display:
  default:
    display_options:
      pager:
        options:
          expose:
            items_per_page_label: 'Items per page'
      fields:
        type:
          label: Type
        operations:
          label: Operations
  checkout:
    display_options:
      fields:
        type:
          label: Type
