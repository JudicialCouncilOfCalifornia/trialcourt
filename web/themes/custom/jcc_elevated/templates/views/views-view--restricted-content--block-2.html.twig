{% for key, subjects in results %}
  <h2>{{ key }}</h2>
  {% for key, topics in subjects %}
    <h3>{{ key }}</h3>
    <ul>
      {% for topic in topics %}
        <li>{{ topic.title }}</li>
      {% endfor %}
    </ul>
  {% endfor %}
{% endfor %}
