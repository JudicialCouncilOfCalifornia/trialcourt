langcode: en
status: true
dependencies:
  config:
    - user.role.surplus_user
  module:
    - user
id: user_remove_role_action.surplus_user
label: 'Remove the Surplus User role from the selected user(s)'
type: user
plugin: user_remove_role_action
configuration:
  rid: surplus_user
