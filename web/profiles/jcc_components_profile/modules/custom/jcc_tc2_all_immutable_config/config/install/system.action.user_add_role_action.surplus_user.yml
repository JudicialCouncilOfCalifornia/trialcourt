langcode: en
status: true
dependencies:
  config:
    - user.role.surplus_user
  module:
    - user
id: user_add_role_action.surplus_user
label: 'Add the Surplus User role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: surplus_user
