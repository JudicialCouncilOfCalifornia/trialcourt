{#
/**
 * @file
 * Theme override to display a book tree.
 *
 * Returns HTML for a wrapper for a book sub-tree.
 *
 * Available variables:
 * - items: A nested list of book items. Each book item contains:
 *   - attributes: HTML attributes for the book item.
 *   - below: The book item child items.
 *   - title: The book link title.
 *   - url: The book link URL, instance of \Drupal\Core\Url.
 *   - is_expanded: TRUE if the link has visible children within the current
 *     book tree.
 *   - is_collapsed: TRUE if the link has children within the current book tree
 *     that are not currently visible.
 *   - in_active_trail: TRUE if the link is in the active trail.
 * - active_trail: active trail
 */
#}

{% if icon_library != "pattern_library" %}
  {% set alt_expand = nav_icon_expand %}
  {% set alt_collapse = nav_icon_collapse %}
{% else %}
  {% set patternlib_expand = nav_icon_expand %}
  {% set patternlib_collapse = nav_icon_collapse %}
{% endif %}

{% include '@molecules/navigation/side-nav/side-nav.twig' with {
  side_nav: {
    aria_label: "Table of Contents",
    expand_icon: {
      name: patternlib_expand,
      width: "width-3",
      height: "height-3"
    },
    collapse_icon: {
      name: patternlib_collapse,
      width: "width-3",
      height: "height-3"
    },
    alt_expand_icon: alt_expand,
    alt_collapse_icon: alt_collapse,
    items: items
  }
} %}
