{#
/**
 * @file
 * Default theme implementation to present a jcc court entity.
 *
 * This template is used when viewing a registered jcc court's page,
 * e.g., /admin/content/jcc-court)/123. 123 being the jcc court's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_jcc_court()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
